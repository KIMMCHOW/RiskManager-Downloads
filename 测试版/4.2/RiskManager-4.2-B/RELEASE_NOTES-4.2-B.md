# RiskManager 4.2-B 发布说明

RiskManager 4.2-B 是 RiskManager 4.2 开发周期中的内部阶段版本。

此版本重点是 HUD 模块化和模块排序。用户可以通过设置控制账户、Tick、风险、价格、订单和状态等模块的显示，并可调整模块顺序。

## 重点说明

- HUD 不再依赖旧的完整 / 精简显示模式。
- HUD 显示由模块化开关和排序控制。
- 模块关闭后 HUD 会自动压缩高度。
- 按钮点击区域会跟随实际渲染位置。
- 此版本不包含风险预设按钮和 MKT / LMT / STP 显示开关。
